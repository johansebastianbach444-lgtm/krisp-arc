krisp-arc/environment.yml
Here's your tailored workflow file:
name: Python Package using Conda

on: [push]

jobs:
  build-linux:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 5

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: '3.10'

    - name: Add conda to system path
      run: echo $CONDA/bin >> $GITHUB_PATH

    - name: Install dependencies
      run: conda env update --file environment.yml --name base

    - name: Lint with flake8
      run: |
        conda install flake8
        # Stop build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # Treat all other issues as warnings
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    - name: Build package
      run: |
        conda install build
        python -m build

    - name: Test with pytest
      run: |
        conda install pytest
        pytest

    - name: Deploy
      run: echo "Add your deploy steps here"
To update the file on your phone:
Go to .github/workflows/main.yml in your repo on GitHub
Tap the pencil (edit) icon
Select all and replace with the above
Scroll down and tap Commit changes
A couple of notes:
The Deploy step just has a placeholder — let me know how/where you deploy (PyPI, a server, etc.) and I can fill that in properly
Make sure your environment.yml is in the root of your repo, since that's where the install step will look for it